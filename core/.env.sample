# Server Configuration
PORT=8080

# Environment Configuration
# Available environments: development, production
NODE_ENV=development

# Logging Configuration
# Available levels: trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# Database Configuration
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/postgres?schema=public"

# Redis Configuration
# Format: redis://HOST:PORT
REDIS_URL="redis://localhost:6379"

# Authentication Configuration
# Secret key for signing tokens -> run bun scripts/generateKey.ts to generate a new key
APP_SECRET="your-very-secure-and-long-secret-key-here"

# Token Expiration Settings
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=30
TOKEN_ISSUER="QuestRider"

# Graceful Shutdown Timeout (in milliseconds)
SHUTDOWN_TIMEOUT=10000
